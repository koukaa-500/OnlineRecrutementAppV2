server:
  port: 5000
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/onlineRecrutement
    username: postgres
    password: nabil1
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
      show-sql: true
  jwt:
    secret: nabil
    expiration: 86400000